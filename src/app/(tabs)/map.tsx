import { ScreenStub } from '@/components/screen-stub';

export default function MapScreen() {
  return (
    <ScreenStub
      title="Map"
      symbol="map"
      blurb="Farm pins across greater Montreal with a bottom-sheet preview on tap. Wiring up react-native-maps next."
    />
  );
}
