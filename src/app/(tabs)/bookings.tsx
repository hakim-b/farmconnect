import { ScreenStub } from '@/components/screen-stub';

export default function BookingsScreen() {
  return (
    <ScreenStub
      title="Bookings"
      symbol="calendar"
      blurb="Your slaughter appointments and split-booking invites will show up here once a request is placed from a farm profile."
    />
  );
}
